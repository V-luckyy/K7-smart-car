# 项目指导文件

本项目的完整指导文档为根目录下的 `CLAUDE.md`（K7 智能小车主板 — RK3576 项目文档）。

**每次开始工作前，必须先完整阅读 `CLAUDE.md`**，并严格遵循其中的约定，特别是：

- 硬件规格与接口约束（如 K7 UART 为 1.8V 电平，与 3.3V 的 STM32 直连会烧板，必须电平转换）
- 本地资料目录结构（`rk3576_data/`、`ROS2_src/`、`K7/`、`camera windows sdk/`）
- 固件烧录、串口调试、SSH 连接等操作流程
- 第 10 节的待确认事项清单

**每次对该项目有新的了解、决策或变更时，必须更新 `CLAUDE.md`**（尤其是待确认事项部分，确认一项划掉一项），保持文档为项目的唯一事实来源。
