# K7 智能小车 — 团队工作日志汇总

---

<a id="sec-update"></a>
## 一、更新规范

### 1.1 记录形式

直接编辑本文档（`work_logs/工作日志汇总.md`），不再生成 PDF。若仍需 PDF，先用 Markdown 写好，再导出为 PDF，文件名：`MMDD日志.pdf`。

### 1.2 每条日志必须包含以下四栏

```markdown
### 日期（例如：2026-07-26）

#### 感知组
- **本周进度**：
  - xxx
- **本周任务**：
  - xxx
- **本周问题**：
  - xxx（没有就写“无”）

#### 控制组
- **本周进度**：...
- **本周任务**：...
- **本周问题**：...

#### 避障组
- **本周进度**：...
- **本周任务**：...
- **本周问题**：...

#### 本周总结
- 跨组协调事项、老师指导意见、下周重点、待决策问题。
```

每周结束后，各组组长按上述模板在 **第二节“进度大纲”末尾** 追加新周次内容，不要另起新文件。

### 1.3 写作要求

- **只写结论和可验证的事实**，不写“看了很多资料”“学了很多东西”等模糊描述。
- **任务必须可验收**：有代码/文件/测试结果/图表即为完成，否则不算完成。
- **问题必须具体**：不要写“有点困难”，要写“A 模块编译报 xxx 错”。
- **待决策问题放到“本周问题”或“本周总结”里**，不再单开章节。

---

<a id="sec-timeline"></a>
## 二、进度大纲

### 0614

#### 感知组
- **本周进度**：完善卡尔曼滤波代码；学习 ROS 以及江协关于 STM32 的知识。
- **本周任务**：继续学习 ROS；了解双目相机的原理以及如何部署。
- **本周问题**：大一基础弱，进度偏慢。

#### 控制组
- **本周进度**：整合卡尔曼滤波以及 APF 算法程序，尝试编译通过。
- **本周任务**：继续学习 ROS 准备路径规划；学习单片机中断知识以及程序整体逻辑；后续主要上机测试程序。
- **本周问题**：程序整体逻辑待吃透。

#### 避障组
- **本周进度**：由于芯片手册问题只进行了 USB 烧录程序，以及完善传感器配置程序。
- **本周任务**：根据正确的芯片手册接线并完成程序烧录，实现传感器数据获取。
- **本周问题**：芯片手册与实际芯片型号不匹配。

#### 本周总结
控制组重点落在程序整体逻辑的理解，以及后续尝试上机测试。感知组目前还是侧重学习 ROS 和双目相机。硬件组需要结合芯片手册完成传感器部署。总体上后续任务推进都围绕程序整体功能上机测试。

**跨组待决策问题**：
- 原 STM32 上的 APF/Stanley/A*/循迹代码是否继续保留在 STM32 内，还是上移到 K7 的 ROS2 层？
- 红外传感器去留：改 STM32 固件把三路 ADC 数据加入上行帧，还是弃用红外、纯深度相机避障？
- 深度相机是否就是 LRCP 2MV？
- 卡尔曼滤波放在哪一层：STM32 层继续跑手写 C 代码，还是 K7 ROS2 层使用 `robot_localization` 标准 EKF 包？

---

### 0607

#### 感知组
- **本周进度**：编写了一个滤波的 C 语言程序，并编写了测试脚本用于测试滤波效果。
- **本周任务**：将滤波合并进主程序；探索使用双目相机等感知传感器；开始学习 ROS2。
- **本周问题**：无。

#### 控制组
- **本周进度**：继续学习 ROS2，学习使用 ROS2 调用电脑摄像头通信；完成合并避障和循迹的程序。
- **本周任务**：和感知组共同确定深度相机的选型；合并感知障碍物定位避障和循迹的代码，调试程序使编译通过。
- **本周问题**：深度相机型号未最终确定。

#### 避障组
- **本周进度**：完成接收三个传感器数据并对障碍物接触点进行定位，实现 C 语言的编写和功能测试。
- **本周任务**：根据小车资源里的原理图对小车电控进行接线还原，实现程序的烧录。
- **本周问题**：无。

#### 本周总结
开始完成从程序到硬件的推进，准备下一步硬件的调试和整体功能程序的整合。

---

### 0531

#### 感知组
- **本周进度**：编写了一个滤波的 Python 程序，实现了简单的滤波功能。
- **本周任务**：将滤波功能封装为一个函数，转换为 C 语言，并为传感器数据预留接口。
- **本周问题**：无。

#### 控制组
- **本周进度**：修复了上次的 bug，编写了 APF 功能和循迹的 C 语言程序。
- **本周任务**：学习 ROS2 系统的通信和工作机制，编写完整的 C 语言 A* 路径规划代码；参考官方项目文件整合已有 C 程序。
- **本周问题**：无。

#### 避障组
- **本周进度**：能够通过 LabVIEW 在小车上实现三个传感器数据的接收；固定好了三个传感器的位置。
- **本周任务**：根据障碍物虚拟定位的思路编写 APF 的 C 语言程序。
- **本周问题**：无。

#### 本周总结
避障组需要着重阅读单片机开发板的接口文档，结合代码完成红外传感器到单片机的连接和初始化。感知组由于还在大一，C 语言和线性代数基础薄弱，老师会帮助进行卡尔曼滤波算法的学习。控制组进度可以，主要学习项目代码，下次尝试在实机上运行调试。

---

### 0524

#### 公共
- **本周进度**：安装 WSL+Ubuntu 系统镜像，配置 ROS2 环境，安装 Keil5 软件，用于阅读小车单片机项目代码。
- **本周任务**：无。
- **本周问题**：无。

#### 感知组
- **本周进度**：无。
- **本周任务**：学习轮趣小车项目文件（STM32）。
- **本周问题**：无。

#### 控制组
- **本周进度**：完善了转角平滑的贝塞尔曲线函数（MATLAB）；发现了一个程序上的 bug，该 bug 导致在结尾的直线路段经过插值生成的路径是弯曲的。
- **本周任务**：完成 C 语言的 APF 功能和循迹的程序编写。
- **本周问题**：路径插值 bug。

#### 避障组
- **本周进度**：编写了避障程序（LabVIEW），实现了分别对三个红外传感器进行切换读取数据，但会出现无法同时读取到数据的问题。
- **本周任务**：调试红外传感器，完成在车身的固定和安装。
- **本周问题**：三个红外传感器无法同时读取数据。

---

### 0517

#### 感知组
- **本周进度**：推导卡尔曼滤波公式（基础薄弱，可能花费时间多点）；写了一个获取小车实时位置并绘制的 vi 程序。
- **本周任务**：结合视频理解卡尔曼滤波；使用 AI 写一个 C 语言的卡尔曼滤波，结合理解。
- **本周问题**：无。

#### 控制组
- **本周进度**：跟着轮趣小车的视频学习了在车上 PID 的原理和实现；了解了车辆横向控制的算法。
- **本周任务**：完善给出的 APF-Stanley 算法，完善转角平滑的贝塞尔曲线函数；尝试用 C 语言复现 APF-Stanley 算法，主要负责 A* 路径规划部分。
- **本周问题**：无。

#### 避障组
- **本周进度**：补买了两个红外传感器，之前买回来的排针没有焊而且读不出数据；完成了 LabVIEW 中获取传感器数据并避障的 vi 程序，尝试加入 APF。
- **本周任务**：尝试编写小车循迹+避障的 C 语言程序。
- **本周问题**：LabVIEW 程序编写中的一些问题已解决。讨论了论文方向（强化学习、深度学习、MPC 等可结合点）。

---

### 0510

#### 感知组
- **本周进度**：了解卡尔曼滤波；完成了一个简单的 vi 程序，对 LabVIEW 有一定了解。
- **本周任务**：完成卡尔曼滤波的公式推导和程序编写；获取小车基于原点的实时坐标并渲染；了解 LabVIEW 程序的封装和调用。
- **本周问题**：无。

#### 控制组
- **本周进度**：调查了 A* 算法和 PP 算法，了解了路径规划和轨迹跟踪的算法特点；跟着视频学习了 PID 的知识。
- **本周任务**：跟着轮趣小车的 PID 视频学习（共 9 节课）；了解 Stanley 算法和 APF 算法。
- **本周问题**：无。

#### 避障组
- **本周进度**：修复了红外传感器采集过程数据溢出的问题；完成了红外传感器数据 myRIO 读取；了解了 APF 算法。
- **本周任务**：考虑三个红外传感器的障碍物测定；完成 APF 避障的 LabVIEW 程序编写（静态目标点）。
- **本周问题**：给定路径分割出的前瞻点在障碍物内部的情况该如何使用 APF。

---

<a id="sec-tasks"></a>
## 三、后续团队任务分配（基于当前 K7_ros2 架构）

> 以下任务基于已确认的硬件方案：K7（RK3576 + Ubuntu 24.04 + ROS2 Jazzy）←USB 串口→ C50X 底盘（STM32F407）；LRCP 2MV USB 双目相机。

### 3.1 控制组（路径规划 + 底盘通信）

| # | 任务 | 产出 | 验收标准 | 依赖 |
|---|------|------|---------|------|
| 1 | Phase 2 收尾：底盘通信验证 | K7 跑通 `k7_core.launch.py` | `/odom`、`/imu/data_raw`、`/PowerVoltage` 均为 20Hz；推车里程计合理 | 最先执行，阻塞其他组 |
| 2 | 看门狗实测 | 测试记录 | 遥控中杀掉 teleop，车 0.5s 内停下 | 任务 1 |
| 3 | STM32 参数确认 | 减速比档位、电池串数、低压行为记录 | 里程计走 1m 误差 <5% | 任务 1 |
| 4 | URDF 装车后修正 | 更新 `k7_description/urdf/k7_robot.urdf` | RViz 模型与实车一致 | 装车完成 |
| 5 | Nav2 集成 | 新建 `k7_nav`：差速参数、`twist_mux`、launch | RViz 给目标点，小车自主到达 | 任务 1、4；感知组深度图 |
| 6 | 局部控制器接口 | 与避障组约定 controller_server 插件接口 | 默认 DWB 可跑；Stanley 可替换 | 任务 5 |

### 3.2 感知组（状态估计 + 视觉）

| # | 任务 | 产出 | 验收标准 | 依赖 |
|---|------|------|---------|------|
| 1 | 双目相机链路 | `k7_camera`：usb_cam 配置 + splitter 节点 + launch | `/camera/left|right/image_raw` 30fps | 控制组 Phase 2 通过 |
| 2 | 双目标定 | 左右目 yaml 存入 `k7_camera/config/` | 重投影误差 <0.5 像素 | 任务 1 |
| 3 | 深度图 | 接入 `stereo_image_proc` | 1m 内点云测距误差 <10% | 任务 2 |
| 4 | EKF 调参 | 优化 `ekf.yaml` | 静止漂移 <1cm/min | 控制组 Phase 2 |
| 5 | IMU 链路验证 | 调优 `imu.yaml` | `/imu/data` 静态不漂 | 任务 4 并行 |
| 6 | NPU 目标检测 | PC 转 YOLOv5→.rknn；`k7_npu` 节点 | `/detections` ≥10fps | Phase 3 完成后 |

### 3.3 避障组（APF + Stanley + 红外/深度）

| # | 任务 | 产出 | 验收标准 | 依赖 |
|---|------|------|---------|------|
| 1 | 红外数据通道决策 | 决策文档：① 改 STM32 固件把三路红外 ADC 加入上行帧；② 弃用红外，纯深度相机避障 | 若选①，`ros2 topic echo` 可见红外距离 | 第一周拍板 |
| 2 | 深度相机→避障输入 | `depthimage_to_laserscan` 或点云障碍层 | `/scan` 与实物距离一致 | 感知组任务 3 |
| 3 | Stanley 控制器迁移 | Nav2 controller 插件或独立节点 | 直线路径横向误差 <5cm | 控制组任务 5 |
| 4 | APF 避障集成 | 局部避障层或速度修正节点 | 1m 内放障碍物，小车绕行不停顿 | 任务 2、3 |
| 5 | 近距避障实测 | 测试报告 | 连续 10 次场景无碰撞 | 任务 4 |

### 3.4 里程碑顺序

```text
控制组 Phase 2 验证 ──→ 感知组相机链路 ──→ 避障组深度输入 ──→ 控制组 Nav2 + 避障组 Stanley/APF ──→ 感知组 NPU
        │                      │
        └──→ 感知组 EKF/IMU 调优（并行）
```

### 3.5 跨组协调规则

- 所有代码必须进入 `ROS2_src/K7_ros2/` 工作区，禁止在 K7 本地放未入库代码。
- 新增依赖必须写进对应包的 `package.xml`，环境安装统一走 `setup_env.sh`。
- 每周组会前，各组组长更新本节“进度大纲”，并把待决策问题写进最新一周的“本周问题”或“本周总结”。

---

<a id="sec-background"></a>
## 四、历史关键决策与背景

阅读旧日志后，需要注意以下背景，它们直接影响当前 `K7_ros2` 架构设计：

1. **原平台是 STM32 + LabVIEW/myRIO**：
   - 控制组在 STM32 上写 C 语言：PID、APF、Stanley、A* 路径规划。
   - 避障组用 LabVIEW/myRIO 读取三个红外传感器。
   - 感知组用 LabVIEW 做卡尔曼滤波和小车位置可视化。
2. **05 月底开始转向 ROS2**：
   - 0524 安装 WSL+Ubuntu+ROS2，学习 ROS2 通信机制。
   - 06 月开始讨论“深度相机选型”，从红外避障向视觉感知过渡。
3. **当前状态（截至 0614）**：
   - 控制组：正在把卡尔曼滤波、APF、Stanley、循迹等代码整合进 STM32 主程序，尝试编译通过。
   - 感知组：卡尔曼滤波 C 代码已完善，开始学习 ROS2 和双目相机。
   - 避障组：红外传感器已完成定位算法和 C 程序，正在解决芯片手册/接线/烧录问题。
4. **与现有 K7 架构的关系**：
   - 原 STM32 上的 APF/Stanley/循迹/A* 需要决定是否保留在 STM32 内，还是上移到 K7 的 ROS2 层。
   - 红外传感器目前固件不上报数据（见 `CLAUDE.md` 13.3），要重新打通或改用深度相机。
   - 卡尔曼滤波在 ROS2 层可用标准包 `robot_localization` 替代，感知组需从“手写 C 代码”转向“调包+调参”。
